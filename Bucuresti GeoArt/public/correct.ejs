<!-- Generated by ChatGPT, modified by JeanM22 -->
<!DOCTYPE html>
<html>
<head>
  <title>GeoArt București</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    * {
      color: <%= color %>;
    }
    .center {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }
    @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap');
    body {
      font-family: 'Roboto', sans-serif;
    }
    div {
      font-size: 1.4em;
      padding: 20px;
      font-weight: 550;
      text-transform: uppercase;
      padding-bottom: 10px;
    }
    input {
      width: 420px;
      font-size: 1.275em;
      padding: 10px;
      text-align: center;
    }
    button {
      line-height: 1;
      font-size: 0.9em;
      padding: 10px;
      margin: 10px;
      font-weight: bold;
    }
    @media only screen and (max-width: 480px) {
      .center {
        flex-direction: column;
      }
      input {
        width: 90%;
      }
    }
    .small {
      font-size: inherit;
    }
  </style>
</head>
<body>
  <div class="center">
    <div class="small">București - <%= displayLetter %>#<%= number %></div>
    <div class="small"><%= topic %></div>
    <div style="color: lime">CORRECT!</div>
    <div><%= coords %></div>
    <% if (bonus_id != -1) { %>
      <code>Number for Bonus #<%= bonus_id %>: <%= bonus_amount %></code>
    <% } else { %>
      <code>A clue to the final bonus can be found inside the cache!</code>
    <% } %>
  </div>
</body>
</html>
